package blog

import (
	"facebookhtmx/views/layouts"
)

templ Blog() {
    @layouts.Base("blog") {
        @Partial()
    }
}

templ Partial() {
    <div id="content" hx-swap-oob="true" class="flex items-start justify-center h-full bg-gray-50">
        <div class="text-xl border border-red-400 text-blue-500 font-bold">
            Blog Page
        </div>
    </div>
}